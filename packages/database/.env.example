# PostgreSQL Database URL
# Run `bun run dev:db` from the root to start and configure a local PostgreSQL instance
# For local development with Homebrew PostgreSQL, use your macOS username
DATABASE_URL=postgresql://your_username@localhost:5432/myapp

# For Docker-based development
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/myapp
